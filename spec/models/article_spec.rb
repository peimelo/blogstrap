require 'rails_helper'

RSpec.describe Article, type: :model do
  it 'validacao inicial' do
    expect(1).to eq(1)
  end

  it 'validacao que vai dar erro' do
    expect(1).to eq(2)
  end

  it 'validacao inicial' do
    expect(1).to eq(1)
  end

  it 'validacao inicial' do
    expect(1).to eq(1)
  end
end
